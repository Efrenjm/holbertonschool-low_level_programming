#include "main.h"
#include <string.h>
/**
 * _strlen - swap the values of two integers
 * @s: string that will be counted 
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
